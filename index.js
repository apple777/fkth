// Entry point for npm consumers. In a Next.js app this file is optional but
// allows tools expecting a Common entry to resolve the package.
export { default as noop } from './src/index.js';

// If you don't expose a library API yet, export an empty object as fallback.
export default {};
